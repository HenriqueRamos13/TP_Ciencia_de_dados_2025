# Worten Scraper Configuration

# Scraping Settings
WORTEN_BASE_URL=https://www.worten.pt
INITIAL_PRODUCTS_LIMIT=10

# Browser Settings
HEADLESS=false
BROWSER_TIMEOUT=60000
WAIT_AFTER_LOAD=3000
WAIT_AFTER_COOKIE=10000

# Database Settings
DB_HOST=localhost
DB_PORT=5432
DB_NAME=worten_scraper
DB_USER=postgres
DB_PASSWORD=postgres

# Output Settings
JSON_OUTPUT_FILE=./scrapper/products.json
